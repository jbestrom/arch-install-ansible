---
- hosts: localhost
  name: bootstrap Arch Linux BTW system

  vars_prompt:
    - name: user_password
      prompt: "user password:"
      encrypt: "sha512_crypt"
      private: yes
      confirm: yes

  tasks:
    - import_role:
      name: archinstall
